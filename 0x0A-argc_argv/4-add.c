#include<stdio.h>
#include<stdlib.h>
/**
 * main - Add positive numbers + } =
 *
 * @argc: number of parameters given to the program
 * @argc: array of pointers to the string of the given parameters
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
int i;

/* If no number is passed as argument */
if (argc == 1)
{
printf("%d\n",0);
}

for(i = 1; i < argc; i++)
{
if (*(argv + i)
}

}
